git add .                             // ADDS all unstaged changes
git commit -m "put anything here"     // Commits staged changes
git config rebase.pull false          // Tells git that it should merge pulled changes
git pull                              // Pulls everything from cloud
git push                              // Pushes commits to cloud